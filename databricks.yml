bundle:
  name: clickstream-kitchen

resources:
  jobs:
    clickstream_etl:
      name: clickstream-etl
      tasks:
        - task_key: run_etl
          notebook_task:
            notebook_path: ./src/etl_job.py

targets:
  dev:
    mode: development
    default: true